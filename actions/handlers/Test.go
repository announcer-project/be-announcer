package handlers

import (
	"github.com/labstack/echo/v4"
)

func CheckUser(c echo.Context) error {
	// authorization := c.Request().Header.Get("Authorization")
	// jwt := string([]rune(authorization)[7:])
	// tokens, _ := DecodeJWT(jwt)
	// db := database.Open()
	// defer db.Close()
	// user := models.
	// db. Where("id = ?", tpkens["user_id"])
	return nil
}
